interface:
  display_name: "External Agent Runner"
  short_description: "Delegate CLI tasks with progress, isolation, and review"
  default_prompt: "Use $runner:sub-agents to delegate this task to Cursor or Grok, monitor the result, and verify the deliverables."

policy:
  allow_implicit_invocation: true
